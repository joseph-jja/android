<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.backup.screens"
    android:versionCode="1"
    android:versionName="1.0">

    <application
        android:debuggable="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name">
        <activity
            android:name=".HomeScreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TabScreen"
            androidLlabel="@string/app_name"></activity>
        <activity
            android:name=".SmsScreen"
            androidLlabel="@string/app_name"></activity>
        <activity
            android:name=".ApplicationScreen"
            androidLlabel="@string/app_name"></activity>
        <activity
            android:name=".WallpaperScreen"
            androidLlabel="@string/app_name"></activity>
    </application>

    <uses-sdk android:minSdkVersion="8" />

    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.WRITE_SMS"/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <!--  useless permission as it only gives you read permissions
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    -->

</manifest> 
